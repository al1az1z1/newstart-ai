# --- Data collection (Task 2) ---
requests>=2.32          # HTTP client for downloading PDFs and listing pages
beautifulsoup4>=4.12    # HTML parsing to find document links on agency pages
lxml>=5.2               # fast parser backend used by BeautifulSoup

# --- Text extraction (Task 3) ---
pymupdf>=1.24           # PyMuPDF ("fitz") -- extracts text from digital PDFs

# --- Dataset build + EDA (Task 4, Task 5) ---
pandas>=2.2             # tabular handling for documents.csv
matplotlib>=3.9         # EDA charts
seaborn>=0.13            # EDA charts (class balance, distributions)

# --- Shared ---
tqdm>=4.66              # progress bars for crawling/extraction loops

# --- Notebooks (notebooks/) ---
jupyter>=1.0            # notebook server + kernel
ipykernel>=6.29          # Python kernel used by the notebooks

# --- Tests (tests/) ---
pytest>=8.0              # unit tests; all HTTP calls are mocked, no live requests
